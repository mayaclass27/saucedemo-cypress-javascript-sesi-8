class loginLocators {
    username = "#user-name"
    password = "#password"
    login_btn = "[data-test='login-button']"
    error_msg = ".api-error"
    title = ".title"
}

export default new loginLocators()